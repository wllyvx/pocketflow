{
  "name": "@pocketflow/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply pocketflow-db --local"
  },
  "dependencies": {
    "@pocketflow/shared": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.7.11",
    "jose": "^6.2.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250813.0",
    "@playwright/test": "^1.62.1",
    "@types/better-sqlite3": "^9.6.0",
    "better-sqlite3": "^13.0.3",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.27.0"
  }
}
