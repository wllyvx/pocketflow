<nav class="fixed inset-x-0 bottom-0 z-10 hidden h-[68px] justify-around border-t border-[#E5E9E5] bg-white/95 px-2 py-2 shadow-[0_-6px_18px_rgba(45,61,69,.06)] max-[680px]:flex" aria-label="Mobile navigation">
  {['◫:Overview:#overview', '↔:Transactions:#transactions', '▣:Envelopes:#envelopes', '⌁:Insights:#insights'].map((item, index) => { const [icon, label, href] = item.split(':'); return <a class:list={['mobile-item flex flex-1 flex-col items-center gap-1.5 text-[9px] font-semibold text-[#8995A0] no-underline', { 'text-coral-600': index === 0 }]} href={href}><span class="text-[19px] leading-[19px]">{icon}</span>{label}</a>; })}
</nav>
<button class="fixed bottom-[84px] right-5 z-10 hidden h-[47px] w-[47px] place-items-center rounded-full border-4 border-[#F5F6F2] bg-coral-500 text-[27px] font-light text-white shadow-[0_7px_17px_rgba(208,93,67,.3)] max-[680px]:grid" type="button" aria-label="Add transaction">+</button>
